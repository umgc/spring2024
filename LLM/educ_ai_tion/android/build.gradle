buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Make sure to use the latest version of the Gradle plugin
        // that is compatible with your version of the Android Gradle plugin.
        classpath 'com.android.tools.build:gradle:8.1.3' // Gradle plugin
        classpath 'com.google.gms:google-services:4.4.0'  // Google Services plugin
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
